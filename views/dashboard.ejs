<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GoFarAsYouCan</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">



    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-60TX6711V8"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-60TX6711V8');
    </script>



    <style>
        /* Base Styles with Futuristic Dark Theme */
        :root {
          --primary-color: #45178f;
          --primary-dark: #5b21b6;
          --primary-light: #ffffffb9;
          --secondary-color: #10b981;
          --secondary-dark: #059669;
          --accent-color: #f97316;
          --text-color: #e2e8f0;
          --text-light: #cbd5e1;
          --text-bright: #ffffff;
          --background-dark: #0f172a;
          --background-darker: #0a0f1d;
          --background-card: #1e293b;
          --background-alt: #1e293b;
          --border-color: #334155;
          --success-color: #05432e;
          --warning-color: #f59e0b;
          --error-color: #ef4444;
          --border-radius: 8px;
          --box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
          --glow-purple: 0 0 15px rgba(109, 40, 217, 0.5);
          --glow-green: 0 0 15px rgba(16, 185, 129, 0.5);
        }

        * {
          margin: 0;
          padding: 0;
          box-sizing: border-box;
        }

        body {
          font-family: 'Poppins', sans-serif;
          color: var(--text-color);
          line-height: 1.6;
          background-color: var(--background-dark);
          background-image: 
            radial-gradient(circle at 10% 20%, rgba(109, 40, 217, 0.1) 0%, transparent 20%),
            radial-gradient(circle at 90% 80%, rgba(16, 185, 129, 0.1) 0%, transparent 20%);
          background-attachment: fixed;
        }

        a {
          color: var(--primary-light);
          text-decoration: none;
          transition: color 0.3s ease;
        }

        a:hover {
          color: var(--text-bright);
        }

        ul {
          list-style: none;
        }

        img {
          max-width: 100%;
          height: auto;
        }

        .container {
          width: 100%;
          max-width: 1200px;
          margin: 0 auto;
          padding: 0 20px;
        }

        /* Typography */
        h1, h2, h3, h4, h5, h6 {
          font-weight: 600;
          line-height: 1.3;
          margin-bottom: 1rem;
          color: var(--text-bright);
        }

        h1 {
          font-size: 2.5rem;
        }

        h2 {
          font-size: 2rem;
        }

        h3 {
          font-size: 1.5rem;
        }

        h4 {
          font-size: 1.25rem;
        }

        p {
          margin-bottom: 1rem;
        }

        /* Futuristic Elements */
        .glow-text {
          text-shadow: 0 0 10px rgba(255, 255, 255, 0.5);
        }

        .gradient-text {
          background: linear-gradient(90deg, var(--primary-light), var(--secondary-color));
          -webkit-background-clip: text;
          -webkit-text-fill-color: transparent;
          background-clip: text;
        }

        /* Buttons */
        .btn {
          display: inline-block;
          background: linear-gradient(135deg, var(--primary-color), var(--primary-dark));
          color: white;
          padding: 0.75rem 1.5rem;
          border-radius: var(--border-radius);
          font-weight: 500;
          border: none;
          cursor: pointer;
          transition: all 0.3s ease;
          text-align: center;
          position: relative;
          overflow: hidden;
          box-shadow: var(--glow-purple);
        }

        .btn:hover {
          transform: translateY(-3px);
          box-shadow: 0 0 20px rgba(109, 40, 217, 0.7);
        }

        .btn::before {
          content: '';
          position: absolute;
          top: 0;
          left: -100%;
          width: 100%;
          height: 100%;
          background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
          transition: 0.5s;
        }

        .btn:hover::before {
          left: 100%;
        }

        .btn-small {
          padding: 0.5rem 1rem;
          font-size: 0.875rem;
        }

        .btn-outline {
          background: transparent;
          border: 1px solid var(--primary-color);
          color: var(--primary-light);
          box-shadow: none;
        }

        .btn-outline:hover {
          background: linear-gradient(135deg, var(--primary-color), var(--primary-dark));
          color: white;
          box-shadow: var(--glow-purple);
        }

        .btn-primary {
          background: linear-gradient(135deg, var(--primary-color), var(--primary-dark));
          color: white;
        }

        /* Navigation */
        .navbar {
          background-color: rgba(15, 23, 42, 0.8);
          backdrop-filter: blur(10px);
          box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
          position: sticky;
          top: 0;
          z-index: 100;
          border-bottom: 1px solid var(--border-color);
        }

        .nav-container {
          display: flex;
          justify-content: space-between;
          align-items: center;
          height: 80px;
        }

        .logo h1 {
          font-size: 1.5rem;
          margin-bottom: 0;
          background: linear-gradient(90deg, var(--primary-light), var(--secondary-color));
          -webkit-background-clip: text;
          -webkit-text-fill-color: transparent;
          background-clip: text;
          text-shadow: 0 0 10px rgba(109, 40, 217, 0.3);
        }

        .nav-menu {
          display: flex;
          align-items: center;
        }

        .nav-menu li {
          margin-left: 1.5rem;
        }

        .nav-menu a {
          color: var(--text-light);
          font-weight: 500;
          position: relative;
        }

        .nav-menu a::after {
          content: '';
          position: absolute;
          bottom: -5px;
          left: 0;
          width: 0;
          height: 2px;
          background: linear-gradient(90deg, var(--primary-light), var(--secondary-color));
          transition: width 0.3s ease;
        }

        .nav-menu a:hover::after, .nav-menu a.active::after {
          width: 100%;
        }

        .nav-menu a:hover, .nav-menu a.active {
          color: var(--text-bright);
        }

        .menu-toggle {
          display: none;
          flex-direction: column;
          cursor: pointer;
        }

        .menu-toggle span {
          width: 25px;
          height: 3px;
          background-color: var(--text-color);
          margin-bottom: 5px;
          border-radius: 2px;
        }

        /* Hero Section */
        .hero {
          background: linear-gradient(135deg, rgba(35, 12, 72, 0.8) 0%, rgba(11, 66, 88, 0.8) 100%);
          padding: 120px 0;
          text-align: center;
          color: white;
          position: relative;
          overflow: hidden;
        }


        .hero-content {
          max-width: 800px;
          margin: 0 auto;
          position: relative;
          z-index: 1;
        }

        .hero h1 {
          font-size: 3.5rem;
          margin-bottom: 1.5rem;
          text-shadow: 0 0 20px rgba(0, 0, 0, 0.3);
          letter-spacing: 1px;
        }

        .hero p {
          font-size: 1.25rem;
          color: rgba(255, 255, 255, 0.9);
          margin-bottom: 2rem;
          text-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }

        .hero .btn {
          background: rgba(119, 16, 16, 0.2);
          backdrop-filter: blur(5px);
          border: 1px solid rgba(255, 255, 255, 0.3);
          color: white;
          box-shadow: 0 0 15px rgba(255, 255, 255, 0.3);
        }

        .hero .btn:hover {
          background: rgba(0, 0, 0, 0.3);
          box-shadow: 0 0 20px rgba(255, 255, 255, 0.5);
        }

        /* Section Styling */
        section {
          padding: 80px 0;
          position: relative;
        }

        .section-header {
          text-align: center;
          margin-bottom: 60px;
        }

        .section-header h2 {
          position: relative;
          display: inline-block;
          padding-bottom: 15px;
          font-size: 2.2rem;
          background: linear-gradient(90deg, var(--primary-light), var(--secondary-color));
          -webkit-background-clip: text;
          -webkit-text-fill-color: transparent;
          background-clip: text;
        }

        .section-header h2::after {
          content: '';
          position: absolute;
          bottom: 0;
          left: 50%;
          transform: translateX(-50%);
          width: 80px;
          height: 3px;
          background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
          box-shadow: 0 0 10px rgba(109, 40, 217, 0.5);
        }

        .section-header p {
          color: var(--text-light);
          max-width: 700px;
          margin: 1rem auto 0;
        }

        /* Course Cards */
        .courses-grid {
          display: grid;
          grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
          gap: 30px;
        }

        .course-card {
          background: linear-gradient(145deg, var(--background-card), var(--background-darker));
          border-radius: var(--border-radius);
          box-shadow: var(--box-shadow);
          padding: 30px;
          text-align: center;
          transition: transform 0.3s ease, box-shadow 0.3s ease;
          border: 1px solid var(--border-color);
          position: relative;
          overflow: hidden;
        }

        .course-card::before {
          content: '';
          position: absolute;
          top: 0;
          left: 0;
          width: 100%;
          height: 5px;
          background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
        }
/* 
        .course-card:hover {
          transform: translateY(-10px);
          box-shadow: 0 15px 30px rgba(35, 17, 17, 0.4);
        } */

        .course-icon {
          width: 100px;
          height: 100px;
          margin: 0 auto 20px;
          display: flex;
          align-items: center;
          justify-content: center;
          border-radius: 50%;
          background: rgba(255, 255, 255, 0.05);
          border: 1px solid rgba(255, 255, 255, 0.1);
          transition: all 0.3s ease;
          position: relative;
        }

        .course-icon::after {
          content: '';
          position: absolute;
          top: -5px;
          left: -5px;
          right: -5px;
          bottom: -5px;
          border-radius: 50%;
          background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
          z-index: -1;
          opacity: 0;
          transition: opacity 0.3s ease;
        }

        .course-card:hover .course-icon {
          transform: scale(1.1);
          box-shadow: 0 0 15px rgba(109, 40, 217, 0.5);
        }

        .course-card:hover .course-icon::after {
          opacity: 0.3;
        }

        .course-icon img {
          width: 60px;
          height: 60px;
          object-fit: contain;
          filter: invert(1) brightness(1.5);
        }

        .course-card h3 {
          margin-bottom: 15px;
          color: var(--primary-light);
        }

        .course-card p {
          color: var(--text-light);
          margin-bottom: 20px;
        }

        /* Development Section */
        .development-section {
          background-color: var(--background-darker);
          position: relative;
          overflow: hidden;
        }

        .development-section::before {
          content: '';
          position: absolute;
          top: 0;
          left: 0;
          width: 100%;
          height: 100%;
          background-image: 
            radial-gradient(circle at 10% 10%, rgba(109, 40, 217, 0.1) 0%, transparent 20%),
            radial-gradient(circle at 90% 90%, rgba(16, 185, 129, 0.1) 0%, transparent 20%);
          z-index: 0;
        }

        /* Resources Grid */
        .resources-grid {
          display: grid;
          grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
          gap: 20px;
          position: relative;
          z-index: 1;
        }

        .resource-card {
          background: linear-gradient(145deg, var(--background-card), var(--background-darker));
          border-radius: var(--border-radius);
          padding: 25px;
          box-shadow: var(--box-shadow);
          transition: transform 0.3s ease, box-shadow 0.3s ease;
          border: 1px solid var(--border-color);
          position: relative;
          overflow: hidden;
        }

        .resource-card::before {
          content: '';
          position: absolute;
          top: 0;
          left: 0;
          width: 4px;
          height: 100%;
          background: linear-gradient(to bottom, var(--primary-color), var(--secondary-color));
        }

        .resource-card:hover {
          transform: translateY(-5px);
          box-shadow: 0 10px 20px rgba(0, 0, 0, 0.4);
        }

        .resource-card h3 {
          margin-bottom: 10px;
          color: var(--primary-light);
          padding-left: 10px;
        }

        .resource-card p {
          color: var(--text-light);
          margin-bottom: 15px;
          padding-left: 10px;
        }

        .link-arrow {
          display: flex;
          align-items: center;
          font-weight: 500;
          color: var(--primary-light);
          padding-left: 10px;
        }

        .link-arrow::after {
          content: "→";
          margin-left: 5px;
          transition: transform 0.3s ease;
        }

        .link-arrow:hover {
          color: var(--text-bright);
        }

        .link-arrow:hover::after {
          transform: translateX(5px);
        }

        /* Footer */
        .footer {
          background-color: var(--background-darker);
          color: var(--text-color);
          padding: 60px 0 20px;
          position: relative;
          overflow: hidden;
        }

        .footer::before {
          content: '';
          position: absolute;
          top: 0;
          left: 0;
          width: 100%;
          height: 1px;
          background: linear-gradient(90deg, transparent, var(--primary-color), var(--secondary-color), transparent);
        }

        .footer-content {
          display: flex;
          flex-wrap: wrap;
          justify-content: space-between;
          margin-bottom: 40px;
          position: relative;
          z-index: 1;
        }

        .footer-logo {
          flex: 0 0 100%;
          margin-bottom: 30px;
        }

        .footer-logo h2 {
          margin-bottom: 10px;
          background: linear-gradient(90deg, var(--primary-light), var(--secondary-color));
          -webkit-background-clip: text;
          -webkit-text-fill-color: transparent;
          background-clip: text;
        }

        .footer-logo p {
          color: var(--text-light);
        }

        .footer-links {
          display: flex;
          flex-wrap: wrap;
          justify-content: space-between;
          width: 100%;
        }

        .footer-column {
          flex: 0 0 calc(33.333% - 20px);
          margin-bottom: 20px;
        }

        .footer-column h3 {
          margin-bottom: 20px;
          font-size: 1.1rem;
          color: var(--text-bright);
          position: relative;
          padding-bottom: 10px;
        }

        .footer-column h3::after {
          content: '';
          position: absolute;
          bottom: 0;
          left: 0;
          width: 40px;
          height: 2px;
          background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
        }

        .footer-column ul li {
          margin-bottom: 10px;
        }

        .footer-column ul li a {
          color: var(--text-light);
          transition: all 0.3s ease;
          display: inline-block;
        }

        .footer-column ul li a:hover {
          color: var(--text-bright);
          transform: translateX(5px);
        }

        .footer-bottom {
          text-align: center;
          padding-top: 20px;
          border-top: 1px solid rgba(255, 255, 255, 0.05);
          position: relative;
          z-index: 1;
        }

        .footer-bottom p {
          color: var(--text-light);
          font-size: 0.9rem;
          margin-bottom: 0;
        }

        /* Futuristic UI Elements */
        .glow-border {
          position: relative;
        }

        .glow-border::after {
          content: '';
          position: absolute;
          top: -2px;
          left: -2px;
          right: -2px;
          bottom: -2px;
          z-index: -1;
          background: linear-gradient(45deg, var(--primary-color), var(--secondary-color));
          border-radius: calc(var(--border-radius) + 2px);
          opacity: 0;
          transition: opacity 0.3s ease;
        }

        .glow-border:hover::after {
          opacity: 1;
        }

        /* Responsive Styles */
        @media (max-width: 992px) {
          .overview-grid {
            grid-template-columns: 1fr;
          }
          
          .overview-sidebar {
            position: static;
          }
          
          .contact-grid {
            grid-template-columns: 1fr;
          }
        }

        @media (max-width: 768px) {
          .nav-menu {
            position: fixed;
            top: 80px;
            left: -100%;
            width: 100%;
            height: calc(100vh - 80px);
            background-color: var(--background-dark);
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            padding-top: 40px;
            transition: left 0.3s ease;
            z-index: 99;
            backdrop-filter: blur(10px);
          }
          
          .nav-menu.active {
            left: 0;
          }
          
          .nav-menu li {
            margin: 15px 0;
          }
          
          .menu-toggle {
            display: flex;
          }
          
          .hero h1 {
            font-size: 2.5rem;
          }
          
          .resource-item, .tutorial-item, .material-item {
            flex-direction: column;
          }
          
          .resource-image, .tutorial-video, .material-image {
            flex: 0 0 200px;
          }
          
          .form-row {
            flex-direction: column;
            gap: 0;
          }
        }

        @media (max-width: 576px) {
          section {
            padding: 60px 0;
          }
          
          .hero {
            padding: 80px 0;
          }
          
          .hero h1 {
            font-size: 2rem;
          }
          
          .section-header {
            margin-bottom: 30px;
          }
          
          .tabs-header {
            flex-wrap: wrap;
          }
          
          .tab-btn {
            flex: 1 0 auto;
            text-align: center;
          }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar">
        <div class="container nav-container">
            <div class="logo">
                <h1>GoFarAsYouCan</h1>
            </div>
            <div class="menu-toggle">
                <span></span>
                <span></span>
                <span></span>
            </div>
            <ul class="nav-menu">
                <li><a href="#home" class="active">Home</a></li>
                <li><a href="#courses">Resources</a></li>
                <li><a href="#fooot">About Us</a></li>
                <li><a href="/signup" class="btn-small btn-primary">Sign Up</a></li>
            </ul>
        </div>
    </nav>

    <!-- Hero Section -->
    <header class="hero" id="home">
        <div class="container">
            <div class="hero-content">
                <h1 class="glow-text">Go Beyond Limits</h1>
                <h2 style="color: cyan;">Check out the Engineering section. The remaining content will be added soon...</h2>
                <p>Write Your Own Destiny and Make It Legendary</p>
                <a href="#courses" class="btn">Explore Courses</a>
            </div>
        </div>
    </header>

    <!-- Main Courses Section -->
    <section id="courses" class="courses-section">
        <div class="container">
            <div class="section-header">
                <h2>Explore Educational Paths</h2>
                <p>Discover various educational opportunities tailored to your interests and career goals</p>
            </div>

            <div class="courses-grid">
                <!-- Engineering Card -->
                <div class="course-card glow-border">
                    <div class="course-icon">
                        <img src="https://img.icons8.com/ios/100/000000/engineering.png" alt="Engineering">
                    </div>
                    <h3>Engineering</h3>
                    <p>Explore various engineering disciplines and build a career in technology and innovation.</p>
                    <a href="/Engineering/engineering.ejs" class="btn-outline" style="padding: 5px; border-radius: 10%; border: 1px solid blueviolet;">Explore Engineering</a>
                </div>

                <!-- Medical Card -->
                <div class="course-card glow-border">
                    <div class="course-icon">
                        <img src="https://img.icons8.com/ios/100/000000/stethoscope.png" alt="Medical">
                    </div>
                    <h3>Medical Sciences</h3>
                    <p>Discover opportunities in healthcare, medicine, and related fields to serve humanity.</p>
                    <a href="#" class="btn-outline" style="padding: 5px; border-radius: 10%; border: 1px solid blueviolet;">Explore Medical</a>
                </div>

                <!-- Commerce Card -->
                <div class="course-card glow-border">
                    <div class="course-icon">
                        <img src="https://img.icons8.com/ios/100/000000/business.png" alt="Commerce">
                    </div>
                    <h3>Commerce & Business</h3>
                    <p>Learn about business, finance, economics, and entrepreneurship opportunities.</p>
                    <a href="#" class="btn-outline" style="padding: 5px; border-radius: 10%; border: 1px solid blueviolet;">Explore Commerce</a>
                </div>

                <!-- Humanities Card -->
                <div class="course-card glow-border">
                    <div class="course-icon">
                        <img src="https://img.icons8.com/ios/100/000000/literature.png" alt="Humanities">
                    </div>
                    <h3>Humanities & Arts</h3>
                    <p>Explore literature, history, philosophy, fine arts, and social sciences.</p>
                    <a href="#" class="btn-outline" style="padding: 5px; border-radius: 10%; border: 1px solid blueviolet;">Explore Humanities</a>
                </div>
            </div>
        </div>
    </section>

    <!-- Personal Development Section -->
    <section class="development-section">
        <div class="container">
            <div class="section-header">
                <h2>Personal Development Resources</h2>
                <p>Enhance your skills beyond academics with these essential resources</p>
            </div>

            <div class="resources-grid">
                <div class="resource-card">
                    <h3>Resume Building</h3>
                    <p>Learn how to create professional resumes using modern tools like Overleaf.</p>
                    <a href="/Resume/resume.ejs" class="link-arrow">Learn More</a>
                </div>

                <div class="resource-card">
                    <h3>LinkedIn Profile</h3>
                    <p>Optimize your LinkedIn profile to stand out to recruiters and build your network.</p>
                    <a href="/Linkedin/linkedin.ejs" class="link-arrow">Learn More</a>
                </div>

                <div class="resource-card">
                    <h3>Communication Skills</h3>
                    <p>Improve your verbal and written communication for academic and professional success.</p>
                    <a href="/Communication/communication.ejs" class="link-arrow">Learn More</a>
                </div>

                <div class="resource-card">
                    <h3>Soft Skills</h3>
                    <p>Develop essential interpersonal skills like teamwork, adaptability, and more to thrive in any professional environment.</p>
                    <a href="/Soft/softskills.ejs" class="link-arrow">Learn More</a>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer" id="fooot">
        <div class="container">
            <div class="footer-content">
                <div class="footer-logo">
                    <h2>GoFarAsYouCan</h2>
                    <p>Guiding students to their perfect educational path</p>
                </div>
                <div class="footer-links">
                    <div class="footer-column">
                        <h3>Quick Links</h3>
                        <ul>
                            <li><a href="/">Home</a></li>
                            <li><a href="/about">About Us</a></li>
                            <li><a href="/contact">Contact</a></li>
                            <li><a href="/resources">Resources</a></li>
                        </ul>
                    </div>
                    <div class="footer-column">
                        <h3>Courses</h3>
                        <ul>
                            <li><a href="/courses/engineering">Engineering</a></li>
                            <li><a href="/courses/medical">Medical</a></li>
                            <li><a href="/courses/commerce">Commerce</a></li>
                            <li><a href="/courses/humanities">Humanities</a></li>
                        </ul>
                    </div>
                    <div class="footer-column">
                        <h3>Connect With Us</h3>
                        <ul class="social-links">
                            <li><a href="#"><s>Facebook</s></a></li>
                            <li><a href="#"><s>Twitter</s></a></li>
                            <li><a href="#"><s>Instagram</s></a></li>
                            <li><a href="#"><s>LinkedIn</s></a></li>
                        </ul>
                    </div>
                </div>
            </div>
            <div class="footer-bottom">
                <p>&copy; 2025 GoFarAsYouCan. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Mobile menu toggle
            const menuToggle = document.querySelector('.menu-toggle');
            const navMenu = document.querySelector('.nav-menu');
            
            if (menuToggle && navMenu) {
                menuToggle.addEventListener('click', function() {
                    navMenu.classList.toggle('active');
                }); 
            }
            
            // Smooth scrolling for anchor links
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function(e) {
                    const href = this.getAttribute('href');
                    
                    if (href !== '#') {
                        e.preventDefault();
                        
                        const targetElement = document.querySelector(href);
                        
                        if (targetElement) {
                            window.scrollTo({
                                top: targetElement.offsetTop - 100,
                                behavior: 'smooth'
                            });
                        }
                    }
                });
            });
            
            // Add glowing effect to cards on hover
            const courseCards = document.querySelectorAll('.course-card');
            courseCards.forEach(card => {
                card.addEventListener('mouseenter', function() {
                    this.classList.add('active');
                });
                
                card.addEventListener('mouseleave', function() {
                    this.classList.remove('active');
                });
            });
            
            // Add subtle parallax effect to hero section
            const hero = document.querySelector('.hero');
            window.addEventListener('scroll', function() {
                const scrollPosition = window.scrollY;
                if (scrollPosition < 600) {
                    hero.style.backgroundPosition = `center ${scrollPosition * 0.5}px`;
                }
            });
        });
    </script>
</body>
</html>